"use strict";
(() => {
    // Retornando un string
    function joinName(fName, lName) {
        return `${fName} ${lName}`;
    }
    // Funcion sin retorno
    function printName(name) {
        console.log(name);
    }
})();
